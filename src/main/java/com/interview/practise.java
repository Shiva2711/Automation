package com.interview;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class practise {

	/*
	 * public static void main(String[] args) {
	 * 
	 * System.out.println(practise.getLowerCase("MyName"));
	 * 
	 * }
	 * 
	 * public static int getLowerCase(String str) {
	 * 
	 * char a[] =str.toCharArray();
	 * 
	 * List <String> lowercases =
	 * Arrays.asList(a).stream().allMatch(s->s.toString().substring(getLowerCase(str
	 * )).collect(Collectors.toList())); int c = (int) lowercases.stream().count();
	 * return c; }
	 */

}
